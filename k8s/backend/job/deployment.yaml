apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-service
  namespace: talentlink-local
  labels:
    app: job-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: job-service
  template:
    metadata:
      labels:
        app: job-service
    spec:
      containers:
        - name: job-service
          image: job-service:latest              # 👈 Local Minikube image
          imagePullPolicy: Never                 # 👈 Don’t pull from GHCR
          ports:
            - containerPort: 5000
          env:
            - name: ENVIRONMENT
              value: local
          readinessProbe:
            httpGet:
              path: /api/jobs/health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/jobs/health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 20
